# PRD: 틱택토(3목) 게임

## 1. 개요
- **제품명**: 틱택토 (Tic-Tac-Toe, 3목)
- **형태**: `index.html` 단일 파일. 더블클릭으로 브라우저에서 바로 실행되는 오프라인 웹앱.
  - 외부 CDN, 빌드 도구, 서버, 네트워크 요청 일절 없음 (HTML/CSS/JS 한 파일에 인라인으로 포함).
- **목적**: 친구와 한 화면(한 기기)에서 번갈아 두는 2인용 게임. 누가 이겼는지 헷갈리지 않도록 차례·승패·점수를 항상 명확하게 보여준다.
- **대상 사용자**: 같은 화면 앞에 함께 있는 친구 2명. 온라인 대전, 컴퓨터(AI) 대전, 로그인/저장 기능 불필요.

## 2. 게임 규칙 (확정 사항 — 사용자 지정)
1. 3×3 판. 빈 칸을 누르면 O와 X가 번갈아 놓인다 (O가 먼저 시작). 이미 채워진 칸은 다시 눌러도 아무 일도 일어나지 않는다.
2. 판 위에 "지금 차례: X"처럼 현재 차례를 항상 표시한다.
3. 가로·세로·대각선 중 한 줄(3칸)을 먼저 채우면:
   - "O 승리!" 또는 "X 승리!" 메시지를 보여준다.
   - 이긴 세 칸을 시각적으로 표시(강조)한다.
   - 판을 잠근다 (더 이상 칸을 누를 수 없음).
4. 9칸이 모두 찼는데 이긴 사람이 없으면 "무승부"를 보여주고 판을 잠근다.
   - 마지막(9번째) 칸을 놓아 줄이 완성되는 경우도 정상적으로 승리 처리한다 (무승부보다 승리 판정이 우선).
5. 점수판에 O 승 · X 승 · 무승부 횟수를 항상 표시한다.
6. [다음 판] 버튼을 누르면 점수는 그대로 두고 판(9칸)만 비운다. 새 판은 항상 O부터 시작한다.
7. O와 X는 서로 다른 색으로 구분해서 보여준다.

## 3. 정해지지 않아 이번에 결정한 사항

| 항목 | 결정 |
|---|---|
| [다음 판] 버튼 노출 시점 | 게임 진행 여부와 상관없이 항상 화면에 보이고 클릭 가능. 게임이 끝나기 전에 눌러도 즉시 판을 비우고 새 게임을 시작하며, 이때 점수는 변하지 않는다(미완료 판은 승패/무승부로 집계하지 않음). |
| 차례 표시와 결과 메시지의 관계 | 같은 상태 표시 영역을 공유한다. 게임 진행 중에는 "지금 차례: X" 형태로, 게임이 끝나면 "O 승리!" 또는 "무승부"로 내용이 바뀐다. |
| 승리 강조 방식 | 이긴 3칸의 배경색을 강조색으로 바꾸고, 살짝 커지는 애니메이션 효과를 준다. 외부 라이브러리 없이 순수 CSS로 구현. |
| 이미 채워진 칸 클릭 / 게임 종료 후 칸 클릭 | 아무 동작도 하지 않는다 (에러 메시지나 경고 없음). |
| O/X 색상 | O는 파란색 계열, X는 빨간색/주황색 계열로 구분. 배경과 대비가 뚜렷하도록 선택. |
| 점수 초기화 기능 | 이번 버전에는 포함하지 않는다 (Out of Scope). 새로고침하면 점수가 0으로 초기화된다. |
| 데이터 저장 | 별도 저장 기능 없음. 새로고침 시 점수·판 모두 초기화됨 (localStorage 등 미사용). |
| 첫 판 시작 시 상태 | 페이지를 처음 열면 점수는 모두 0, 빈 9칸, "지금 차례: O"부터 시작. |
| 언어/폰트 | 한국어 UI, 시스템 기본 산세리프 폰트. 차례·결과 메시지는 한눈에 보이도록 큰 글씨(예: 1.5rem 이상) 사용. |
| 반응형 | 데스크톱 브라우저 기준 최소 지원. 모바일 폭에서도 판과 버튼이 화면 밖으로 잘리지 않을 정도의 최소 반응형 처리(뷰포트 메타 태그, 유동 레이아웃)만 적용. |
| 접근성 | 각 칸 버튼에 현재 상태(빈 칸/O/X)를 알 수 있는 텍스트 라벨 제공, 상태 메시지 영역에 `aria-live="polite"` 적용. |
| 브라우저 지원 | 최신 Chrome/Edge 기준 (더블클릭 시 기본 브라우저로 열림을 가정). |
| 마우스/터치 조작 | 클릭(터치)만 지원. 키보드 전용 조작(Tab+Enter로 칸 선택)은 이번 범위에서 제외. |

## 4. 화면 구성 요소
- 제목 (예: "틱택토 ⭕❌")
- 점수판: "O 승: N · X 승: N · 무승부: N"
- 상태 표시 영역: "지금 차례: X" ↔ "O 승리!" / "X 승리!" / "무승부" (공유 영역)
- 3×3 게임판 (9개의 칸 버튼)
- 승리 시 이긴 세 칸 강조 표시
- [다음 판] 버튼

## 5. 비목표 (Out of Scope)
- 온라인 대전, 컴퓨터(AI) 상대 모드
- 로그인, 서버/DB 연동, 순위표
- 점수 초기화, 판 크기 변경(3×3 이외), 난이도 조절
- 사운드/음악, 다국어 지원
- 되돌리기(undo) 기능

## 6. 성공 기준
- `index.html` 파일 하나만 더블클릭해도 인터넷 연결 없이 정상 동작한다.
- 가로·세로·대각선 승리, 무승부(마지막 칸 승리 포함) 판정이 모두 정확히 동작한다.
- 차례 표시, 승패 메시지, 이긴 칸 강조, 점수판 누적, [다음 판]으로 판만 초기화되는 동작이 모두 명확히 확인된다.
- O와 X가 색으로 뚜렷이 구분된다.
